#include "encryptor.h"
#include <assert.h>

void testEncryptLog() {
    encryptLog();
    // Check if log.enc exists
}

int main() {
    testEncryptLog();
    std::cout << "Encryptor tests passed." << std::endl;
    return 0;
}
